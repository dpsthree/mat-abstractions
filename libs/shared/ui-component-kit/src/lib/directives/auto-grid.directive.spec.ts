import { AutoGridDirective } from './auto-grid.directive';

describe('AutoGridDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoGridDirective();
    expect(directive).toBeTruthy();
  });
});
